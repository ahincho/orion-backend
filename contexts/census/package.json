{
  "name": "@orion/context-census",
  "version": "0.0.0",
  "description": "ORION P1 bounded context: Asignacion de Censos (homes, surveys, daily assignments).",
  "private": true,
  "type": "module",
  "main": "./dist/composition.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-package.js",
    "test": "vitest run --root ../.."
  },
  "dependencies": {
    "kysely": "^0.29.4",
    "pg": "^8.13.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/pg": "^8.11.10",
    "typescript": "^6.0.3"
  }
}
